"use client";
import { Stack, styled } from "@mui/material";

const UserItemContainer = styled(Stack)({
  alignItems: "stretch",
  bgcolor: "background.light",
  boxShadow: "0px 2px 4px 0px rgba(0, 0, 0, 0.10)",
  width: "100%",
  borderRadius: "10px",
});

export default UserItemContainer;
