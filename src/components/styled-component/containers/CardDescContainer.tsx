"use client";
import { Stack, styled } from "@mui/material";

const CardDescContainer = styled(Stack)({
  flexDirection: "column",
  gap: "10px",
  padding: "25px 25px 35px 25px",
});

export default CardDescContainer;
